const queries = require('./defQueries');
const mutations = require('./defMutations');
const inputs = require('./defInputs');

const users = require('./users');

module.exports = [, queries, mutations, inputs, users];
